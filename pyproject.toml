[project]
name = "excel-csv-reader"
version = "0.1.0"
description = "Read CSV and Excel files with a small CLI using pandas"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.2",
    "openpyxl>=3.1.5",
    "pandas>=3.0.1",
]
